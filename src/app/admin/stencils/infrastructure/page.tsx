
"use client";

// This page is now a secondary placeholder.
// The main content for infrastructure stencils is temporarily moved to /admin/stencils/page.tsx for debugging.

export default function InfrastructureStencilsPage() {
  return (
    <div className="p-4">
      <h2 className="text-lg">Infrastructure Page Content (Simplified Fallback)</h2>
      <p>This content should ideally not be the primary view if /admin/stencils is working.</p>
    </div>
  );
}
